
# Install

Make sure neovim is installed

```
$cd dotfiles
$git submodule update --init
$./make_links.sh
```

```
#update-alternatives --config editor
```

Open neovim and run
```
:PluginInstall
```

